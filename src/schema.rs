table! {
    cards (id) {
        id -> Varchar,
        name -> Varchar,
        overall_power -> Tinyint,
        class -> Tinyint,
        genre -> Tinyint,
        rarity -> Tinyint,
        image -> Varchar,
    }
}
