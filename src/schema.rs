table! {
    base_cards (id) {
        id -> Varchar,
        name -> Varchar,
        overall_power -> Tinyint,
        class -> Tinyint,
        genre -> Tinyint,
        mal_id -> Integer,
    }
}
